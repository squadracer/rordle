<div class="my-10 text-center flex justify-between">
    <div class="relative flex flex-col justify-between">
        <div class="flex w-full justify-center opacity-0 absolute" id="copy-toast">
            <p class="rounded text-white bottom-0 bg-red-900 p-px">Copied to your clipboard!</p>
        </div>
        <div data-controller="share-answers" data-action="click->share-answers#copyToClipboard" class="text-left bg-red-50 rounded p-2 border-dotted" id="share_text">
            Hey, I have challenged myself with today's RoRdle!
            <br/>
            <% colors.each_with_index do |color_line, index| %>
                <% break if index >= answers_size %>
                <% line = color_line.map {|color| (color == "green" ? "🟩" : (color == "orange" ? "🟧" : "⬜️")) }.join %>
                <%= line %>
                <br/>
            <% end %>
            And you? 😁
        </div>
    </div>
    <a href="/infinite" class="m-auto bg-green-400 text-white font-semibold px-10 py-3 rounded">Play again</a>
</div>